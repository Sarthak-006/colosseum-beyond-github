# colosseum-beyond-github

**Entry point:** [SKILL.md](SKILL.md) — the YAML `name` must match this repository name: `colosseum-beyond-github`.

**Submit:** `git init` in this directory, push to a public GitHub repo with that exact name (root = these files: `SKILL.md`, `assets/`, `references/`, `scripts/`).

**After install (optional):** clone into `.cursor/skills/colosseum-beyond-github/`.

**Local cleanup:** if you still have `SagaPad/.cursor/skills/colosseum-beyond-github/`, close Cursor/terminals using it, then delete that folder so only this tree remains as source of truth.
